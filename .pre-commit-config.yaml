default_install_hook_types: [pre-commit]

repos:
  - repo: local
    hooks:
      - id: touch-commit-stamp
        name: Touch package commit stamp files before commit
        entry: python3 scripts/touch_commit_stamp.py
        language: system
        stages: [pre-commit]
        always_run: true
        pass_filenames: true
