default_install_hook_types: [pre-commit]

repos:
  - repo: local
    hooks:
      - id: touch-stamp-files
        name: Touch package commit stamp files before commit
        entry: python3 scripts/touch_stamp_files.py
        language: system
        stages: [pre-commit]
        always_run: true
        pass_filenames: true
